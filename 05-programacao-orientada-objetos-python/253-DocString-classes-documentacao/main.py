import documentando_class

# observe que a documentação está sendo preenchida tanto com o type hinting (quando construimos a funcao colocando os tipos dos argumentos aceitos e tipo de saida logo no cabeçalho) quanto a documentação normal onde explicamos o funcionamento em texto livre
help(documentando_class)